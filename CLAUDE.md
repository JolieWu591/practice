# my/ 工作空间规范

Jolie 的个人产品和作品目录。这里的东西会对外署名、会长期维护，所以 **不允许在根目录裸放文件**，一切必须落进命名规范的子项目文件夹。

本文件只补充 my/ 特有的约定。

---

## 偷懒协议 (Lazy Dev Protocol)

触发条件：用户在 `my/` 根目录下直接开始开发（要求创建文件、写代码、跑命令），但没指定子项目文件夹。

Claude 必须 **立刻暂停开发**，执行以下流程：

1. **问一句话**：“这是什么，想解决什么问题？” —— 只问一次，不要连环问  
2. **提议命名**：基于回答，给出 1-3 个 kebab-case 英文候选名（如 `pdf-splitter`、`daily-note-bot`），简要说明每个名字的语义差别  
3. **建议文件夹**：用户选定后立即 `mkdir my/<name>/`，把已经产生的裸文件全部 `mv` 进去  
4. **脚手架 CLAUDE.md**：按下方模板生成 `my/<name>/CLAUDE.md`，填入目的、技术栈、启动命令三项即可  
5. **再继续开发**：以上四步完成后，回到用户原始需求  

**不要跳过这个流程**。哪怕用户说“先写个小 demo”，也先命名。`sandbox/` 才是 demo 的地方，`my/` 里的一切默认都要活下去。

---

## 命名判断

给候选名时遵循：

- 优先 **动宾结构或名词短语**：`note-sync`、`link-shortener`，而不是 `tool`、`util`
- 2-3 个单词为宜，单个单词太泛，4 个以上太长
- 避免 `my-xxx`、`kz-xxx` 这种所有权前缀（已经在 my/ 下了）
- 避免版本后缀（`xxx-v2`）、日期后缀（`xxx-2026`）

---

## 子项目 CLAUDE.md 最小模板

```md
# <project-name>

<一句话说明这个项目是什么、给谁用、解决什么问题>

## 技术栈

- 语言 / 框架: 
- 关键依赖: 

## 启动

```bash
<install 命令>
<run 命令>
```

## 部署

（如需要部署说明，在这里写部署方式，例如 Vercel / Docker / 服务器等）
```

---
```